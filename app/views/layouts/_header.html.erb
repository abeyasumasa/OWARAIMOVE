<header class="navbar navbar-fixed-top navbar-inverse">
<div class="container">
  <nav>
    <ul class="nav navbar-nav navbar-right">
      <% if user_signed_in? %>
        <li><%= link_to 'トップ', root_path %></li>
        <li><%= link_to 'ライブ一覧', lives_path %></li>
        <% if current_user.entertainer == true && current_user.comedian != nil %>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
              芸人 <b class="caret"></b>
            </a>
            <ul class="dropdown-menu">
              <li><%= link_to '芸人一覧', comedians_path %></li>
              <li><%= link_to '芸人プロフィール', comedian_path(current_user.comedian.id) %></li>
              <li><%= link_to '芸人プロフィール変更', edit_comedian_path(current_user.comedian.id) %></li>
            </ul>
          <% else %>
            <li><%= link_to '芸人一覧', comedians_path %></li>
          <% end %>
          </li>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown">
            アカウント <b class="caret"></b>
          </a>
          <ul class="dropdown-menu">
            <li><%= link_to current_user.name, user_path(current_user.id) %></li>
            <li><%= link_to 'プロフィール変更', edit_user_path(current_user.id) %></li>
            <li><%= link_to '管理画面', users_path %></li>
            <li class="divider"></li>
              <li><%= link_to 'ログアウト', destroy_user_session_path, method: :delete %></li>
            </li>
          </ul>
      <% else %>
        <li><%= link_to 'サインアップ', new_user_registration_path %></li>
        <li><%= link_to 'ログイン', new_user_session_path %></li>
      <% end %>
    </ul>
  </nav>
</div>
</header>

<p class="notice"><%= notice %></p>
<p class="alert"><%= alert %></p>