<h1>参加する予定のライブの開催が明日です！</h1>
<% @user.participant_management_lives.where(date: Date.today.since(1.days)).each do |live|%>
  <h4>ライブ名: <%= live.title %> </h4>
  <h4>ライブ時間: <%= live.start_time %> </h4>
  <h4>会場: <%= live.place %></h4>
  <h4>料金: <%= live.price %></h4><br>
<% end %>